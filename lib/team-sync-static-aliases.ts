/**
 * Deprecated: the Teams Google Sheet and `public.team_aliases` are the alias source of truth.
 * Kept as an empty export so older imports can be removed gradually.
 */
export const TEAM_ALIAS_CANONICAL_HINTS = [] as const
