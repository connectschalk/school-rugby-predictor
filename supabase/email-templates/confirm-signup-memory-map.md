# Memory Map — confirm signup (reference)

Use the combined template in [confirm-signup.md](./confirm-signup.md) for production.

**Subject:** Confirm your Memory Map account

**Sender (dashboard):** NextPlay Memory Map or NextPlay

Reference HTML is generated by `memoryMapConfirmSignupHtml()` in `lib/auth-email-templates.ts`.

Required sign-up metadata (set by `/memory-map/auth/sign-up`):

```json
{ "signup_product": "memory_map" }
```
