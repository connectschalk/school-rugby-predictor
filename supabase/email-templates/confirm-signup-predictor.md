# Predictor — confirm signup (reference)

Use the combined template in [confirm-signup.md](./confirm-signup.md) for production.

**Subject:** Confirm your NextPlay Predictor account

**Sender (dashboard):** NextPlay Predictor or NextPlay

Reference HTML is generated by `predictorConfirmSignupHtml()` in `lib/auth-email-templates.ts`.

Required sign-up metadata (set by `/signup`):

```json
{ "signup_product": "predictor" }
```
