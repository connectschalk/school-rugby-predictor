/**
 * Backwards-compatible entry: same default export as the shared export card (prediction variant).
 * Logo helpers live under `components/export/` for Studio + public predictor.
 */
export { getTeamLogo, RugbyBallIcon, TeamLogoCircle } from '@/components/export/team-logo'
export { default } from '@/components/export/ExportPredictionCard'
